# The default language
language: en-US

# Whether to use the corresponding language that player uses
detect-player-locale: true

# Update check
check-update: true

# Mine settings
mine:
  # Allow place blocks in mines
  allow-place: false
  # Allow to players earn XP from skill plugins in mines
  # Note: support mcMMO and AuraSkills
  allow-earn-xp: true

# Storage settings
# Require restart
storage:
  # Database type
  # Support: SQLite, MySQL, MariaDB, PostgreSQL
  type: sqlite
  remote:
    host: localhost
    port: 3306
    database: supermines
    username: root
    password: PASSWORD